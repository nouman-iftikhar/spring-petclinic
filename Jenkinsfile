pipeline {
    agent any
    tools {
        docker 'docker'
    }
    stages {
        stage('Docker Build') {
            steps {
                script {
                    sh 'docker --version'
                }
                
            }
        }
    }
}
